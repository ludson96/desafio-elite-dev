# 📋 Plano de Implementação: Cancelamento de Reserva & Devolução ao Estoque

Este plano descreve o passo a passo para implementar o cancelamento seguro de reservas pelo cliente, com estorno atômico de ingressos e devolução da capacidade ao estoque do evento.

---

## 🎯 Regras de Negócio do Cancelamento

1. **Apenas o Cliente Dono**: Somente o usuário que realizou a reserva (`clientId === req.user.id`) pode cancelá-la;
2. **Status Permitido**: Apenas reservas com status `CONFIRMED` e cujos ingressos ainda estejam `ACTIVE` (não utilizados) podem ser canceladas;
3. **Ingressos Já Usados**: Se qualquer ingresso da reserva já tiver sido validado na portaria (`status === 'USED'`), o cancelamento é bloqueado com erro amigável (`400 Bad Request`);
4. **Transação Atômica ACID**:
   - Atualiza `Reservation.status = 'CANCELED'`;
   - Atualiza todos os `Ticket.status = 'CANCELED'`;
   - Devolve a quantidade de ingressos ao evento via `Event.availableTickets: { increment: quantity }`;
5. **Comportamento na Portaria**: Se alguém tentar usar um ingresso cancelado na portaria, o validador já retorna `"Este ingresso foi cancelado."`.

---

## 📁 Arquivos a Serem Modificados / Criados (Execução Passo a Passo)

### 🔹 Passo 1 (Back-End: Repositório)
- **Arquivo**: [`backend/src/repositories/reservation.repository.ts`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/backend/src/repositories/reservation.repository.ts)
- **Ação**: Adicionar o método `cancelReservationWithStockRefund(reservationId, eventId, quantity)` encapsulando a transação do Prisma.

### 🔹 Passo 2 (Back-End: Serviço)
- **Arquivo**: [`backend/src/services/reservation.service.ts`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/backend/src/services/reservation.service.ts)
- **Ação**: Adicionar o método `cancelReservation(reservationId, clientId)` contendo as validações de autorização, integridade e regras de negócio.

### 🔹 Passo 3 (Back-End: Controlador & Rotas)
- **Arquivos**:
  - [`backend/src/controllers/reservation.controller.ts`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/backend/src/controllers/reservation.controller.ts) (adicionar `cancel` handler);
  - [`backend/src/routes/reservation.routes.ts`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/backend/src/routes/reservation.routes.ts) (adicionar rota `PATCH /api/reservations/:id/cancel` protegida para `CLIENT`).

### 🔹 Passo 4 (Back-End: Testes Automatizados)
- **Arquivo**: [`backend/tests/reservation.service.test.ts`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/backend/tests/reservation.service.test.ts)
- **Ação**: Adicionar testes unitários cobrindo cancelamento com sucesso e bloqueio de ingressos já utilizados.

### 🔹 Passo 5 (Front-End: Cliente HTTP & Tipos)
- **Arquivo**: [`frontend/src/services/api.ts`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/frontend/src/services/api.ts)
- **Ação**: Adicionar método `reservationsApi.cancel(id)`.

### 🔹 Passo 6 (Front-End: Interface de Minhas Compras)
- **Arquivo**: [`frontend/src/app/my-reservations/page.tsx`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/frontend/src/app/my-reservations/page.tsx)
- **Ação**: Adicionar botão **"Cancelar Reserva"** nas compras confirmadas, com modal de confirmação, feedback de sucesso e atualização imediata do status.

### 🔹 Passo 7 (Atualização de Toda a Documentação)
- **Arquivos**:
  - [`backend/README.md`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/backend/README.md) (adicionar a nova rota `PATCH /api/reservations/:id/cancel` e regras);
  - [`backend/DECISIONS.md`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/backend/DECISIONS.md) (documentar a estratégia de transação atômica do cancelamento e devolução ao estoque);
  - [`frontend/README.md`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/frontend/README.md) (documentar a interface do fluxo de cancelamento em *Minhas Compras*);
  - [`frontend/DECISIONS.md`](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/frontend/DECISIONS.md) (documentar a UX do modal de confirmação e feedback);
  - [`README.md` (Raiz)](file:///d:/Code/desafios-tecnicos/desafio-elite-dev/README.md) (atualizar destaques, endpoints, roteiro de avaliação e status de testes).

---

## 🧪 Plano de Verificação

1. **Testes Unitários**: Rodar `npm test` no backend e frontend para validar as novas regras;
2. **Teste Manual de Fluxo**:
   - Logar como Cliente e comprar 2 ingressos de um evento;
   - Verificar que `availableTickets` diminuiu em 2;
   - Cancelar a compra em *Minhas Compras*;
   - Verificar que a compra virou `CANCELADO` e o estoque do evento foi restabelecido;
   - Tentar validar o ingresso cancelado na portaria para comprovar o bloqueio.
